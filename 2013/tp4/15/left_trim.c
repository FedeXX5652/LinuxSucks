#include <stdio.h>
#include <string.h>

void left_trim (char *s) {

    int size_t;
    size_t = sizeof(s)/sizeof(s[0]);

    printf("String en funcion: %s\n", s);

    for (int i = 0; i <size_t; i++ ){

        if (s[i] == " "){

            


        }
        
        
    }

}